#include "main.h"
#include<stdio.h>
/**
 * _puts - Prints a message using the puts function
 * @str: The string to be printed
 */
void _puts(char *str);
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)

{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
